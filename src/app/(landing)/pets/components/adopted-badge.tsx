import { Badge } from "@/components/ui/badge";

const AdoptedBadge = () => {
	return (
		<Badge className="bg-primary py-1 px-3 text-white text-sm font-medium tracking-wide rounded-full backdrop-blur-sm">
			Adoptado
		</Badge>
	);
};

export default AdoptedBadge;
